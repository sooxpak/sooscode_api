spring.application.name=sooscode-api

# Server Port
server.port=8080

# REST Base Path
spring.data.rest.basePath=/api

# Encoding Settings
#spring.http.encoding.charset=UTF-8
#spring.http.encoding.enabled=true
#spring.http.encoding.force=true

# MariaDB 11.8/RDS
spring.datasource.url=jdbc:mariadb://fs12-sooscode-api.cfm6wkquapqe.ap-northeast-2.rds.amazonaws.com:3306/sooscode_api?useSSL=true&requireSSL=true&verifyServerCertificate=false
spring.datasource.username=soo
spring.datasource.password=code
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# HikariCP Connection Pool
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000

# JPA Basic Settings
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true
spring.jpa.hibernate.ddl-auto=update
# create, create-drop, update, validate, none

# JPA Performance Optimization
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.jdbc.batch_size=20

# SQL Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# JSON Settings
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=Asia/Seoul

# CORS Settings (Global)
# Alternative: configure in WebConfig
# spring.web.cors.allowed-origins=http://localhost:3000,http://localhost:5173
# spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,PATCH
# spring.web.cors.allowed-headers=*

# Google OAuth Settings
oauth.google.client-id=85857934590-ma6ehp73g3rr5nt9r2opq6st1nco451s.apps.googleusercontent.com
oauth.google.client-secret=aaaa
oauth.google.redirect-uri=http://localhost:8080/api/auth/google/callback

# Gmail SMTP Settings
#spring.mail.host=smtp.gmail.com
#spring.mail.port=587
#spring.mail.username=sooxpak@gmail.com
#spring.mail.password=aaaa

# Gmail SMTP Authentication
#spring.mail.properties.mail.smtp.auth=true
#spring.mail.properties.mail.smtp.starttls.enable=true

# WebSocket & STOMP Configuration
#websocket.endpoint=/ws
#stomp.broker.prefix=/topic,/queue
#stomp.application.prefix=/app
#websocket.allowed.origins=http://localhost:5173

# File Upload Configuration (HTTP Multipart)
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.location=/tmp

# LiveKit Settings (for video streaming - add when needed)
# livekit.url=wss://your-livekit-server
# livekit.api-key=YOUR_API_KEY
# livekit.api-secret=YOUR_API_SECRET

# AWS S3 Settings (for file upload - add when needed)
# cloud.aws.credentials.access-key=YOUR_ACCESS_KEY
# cloud.aws.credentials.secret-key=YOUR_SECRET_KEY
# cloud.aws.region.static=ap-northeast-2
# cloud.aws.s3.bucket=your-bucket-name
# cloud.aws.stack.auto=false

# Docker Execution Settings (for code compilation - add when needed)
# docker.image.java=openjdk:17
# docker.image.python=python:3.11 ??? ??
# docker.timeout=30000



# OpenAI API Settings (for AI tutor - add when needed)
# openai.api-key=YOUR_OPENAI_API_KEY
# openai.model=gpt-4
# openai.max-tokens=2000